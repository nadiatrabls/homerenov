var myCarousel = document.querySelector('#carouselTestimonials');
var carousel = new bootstrap.Carousel(myCarousel, {
  interval: 3000, // Définit un intervalle de 3 secondes entre chaque témoignage
  ride: 'carousel'
});
