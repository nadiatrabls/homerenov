<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nouvelle Demande d'Étude</title>
    <style>
        /* Style de base */
        body {
            font-family: 'Arial', sans-serif;
            color: #333;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Conteneur principal */
        .container {
            max-width: 600px;
            margin: 20px;
            padding: 20px 30px;
            background-color: #ffffff;
            border: 1px solid #ddd;
            border-radius: 10px;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
        }

        /* Titre principal */
        h1 {
            color: #007bff;
            font-size: 24px;
            text-align: center;
            margin-bottom: 20px;
        }

        /* Styles des paragraphes */
        p {
            line-height: 1.6;
            margin: 5px 0;
        }

        /* Mise en forme des éléments d'information */
        .info-item {
            padding: 15px;
            background-color: #f0f8ff;
            border-left: 4px solid #007bff;
            border-radius: 5px;
            margin-top: 10px;
        }

        /* Pied de page */
        .footer {
            margin-top: 20px;
            font-size: 0.9em;
            color: #666;
            text-align: center;
            border-top: 1px solid #ddd;
            padding-top: 15px;
        }

        /* Style des labels */
        strong {
            color: #555;
            font-weight: bold;
        }

        /* Espacement pour chaque information */
        .info-item p {
            margin: 8px 0;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Nouvelle demande d'étude</h1>
        <div class="info-item">
            <p><strong>Type de projet :</strong> {{ projectType }}</p>
            <p><strong>Type de locaux :</strong> {{ localType }}</p>
            <p><strong>Surface des locaux :</strong> {{ surface }} m²</p>
            <p><strong>Budget prévisionnel :</strong> {{ budget }} €</p>
            <p><strong>Localisation du projet :</strong> {{ location }}</p>
        </div>

        <div class="info-item">
            <p><strong>Nom et prénom :</strong> {{ firstName }} {{ lastName }}</p>
            <p><strong>Téléphone :</strong> {{ phone }}</p>
            <p><strong>Email :</strong> {{ contactEmail }}</p>
        </div>

        <div class="info-item">
            <p><strong>Détails du projet :</strong></p>
            <p>{{ projectDetails }}</p>
        </div>

        <div class="footer">
            <p>Ce message a été envoyé depuis le formulaire de demande d'étude de votre site.</p>
        </div>
    </div>
</body>
</html>
