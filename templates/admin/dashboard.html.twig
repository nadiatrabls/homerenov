{% extends 'base.html.twig' %}

{% block title %}Gestion du site{% endblock %}

{% block body %}
<div class="container-fluid my-5 back">
    <div class="card shadow-sm">
        <div class="card-header bg-dark text-white text-center">
            <h2>Tableau de bord administrateur</h2>
        </div>
        <div class="card-body">
            <h5 class="mb-4">Liste des utilisateurs</h5>

            <!-- Rendre le tableau responsive -->
            <div class="table-responsive">
                <table class="table table-bordered table-striped table-hover">
                    <thead class="thead-dark">
                        <tr>
                            <th>Nom</th>
                            <th>Prénom</th>
                            <th>Email</th>
                            <th>Date de naissance</th>
                            <th>Téléphone</th>
                            <th>Adresse</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for user in users %}
                        <tr>
                            <td>{{ user.nom }}</td>
                            <td>{{ user.prenom }}</td>
                            <td>{{ user.email }}</td>
                            <td>{{ user.dateNaissance|date('d/m/Y') }}</td>
                            <td>{{ user.telephone }}</td>
                            <td>{{ user.adressePostale }}</td>
                            <td>
                                <!-- Icône pour voir l'utilisateur -->
                                <a href="{{ path('user_show', {'id': user.id}) }}" class="btn btn-info btn-sm mb-1">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <!-- Icône pour modifier l'utilisateur -->
                                <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-warning btn-sm mb-1">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <!-- Icône pour supprimer l'utilisateur -->
                                <form method="post" action="{{ path('user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');" style="display:inline-block;">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                                    <button class="btn btn-danger btn-sm">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                        {% else %}
                        <tr>
                            <td colspan="7" class="text-center">Aucun utilisateur trouvé.</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

{# <!-- Gestion des rendez-vous -->
<div class="container my-5">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h5>Gestion des rendez-vous</h5>
        </div>
        <div class="card-body">
            <!-- Contrôles du calendrier -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <button id="prevMonthBtn" class="btn btn-outline-primary">Précédent</button>
                <span id="currentMonth" class="h5"></span>
                <button id="nextMonthBtn" class="btn btn-outline-primary">Suivant</button>
            </div>

            <!-- Table du calendrier -->
            <div class="table-responsive">
                <table class="table table-bordered text-center" id="calendar-table">
                    <thead class="thead-light">
                        <tr>
                            <th>Dim</th>
                            <th>Lun</th>
                            <th>Mar</th>
                            <th>Mer</th>
                            <th>Jeu</th>
                            <th>Ven</th>
                            <th>Sam</th>
                        </tr>
                    </thead>
                    <tbody id="calendar-body">
                        <!-- Le calendrier sera généré ici par le JS -->
                    </tbody>
                </table>
            </div>

            <!-- Formulaire de création de rendez-vous -->
            <div class="rendezvous-form mt-4">
                <h6>Créer un Rendez-vous</h6>
                <form id="rendezvous-form" class="d-flex align-items-center">
                    <input type="text" id="selectedDate" class="form-control me-2" placeholder="Date" readonly>
                    <label for="heureDebut" class="me-2">Heure de début :</label>
                    <input type="time" id="heureDebut" class="form-control me-2">
                    <label for="heureFin" class="me-2">Heure de fin :</label>
                    <input type="time" id="heureFin" class="form-control me-2">
                    <button type="submit" class="btn btn-success">Ajouter</button>
                </form>
            </div>
        </div>
    </div>
</div> #}


    <!-- Bouton pour accéder au calendrier et gestion des disponibilités -->
    <div class="text-center mt-5">
        <a href="{{ path('admin_disponibilites') }}" class="btn btn-primary">Gérer les Disponibilités</a>
    </div>
</div>


{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/calendar.js') }}"></script>
{% endblock %}
